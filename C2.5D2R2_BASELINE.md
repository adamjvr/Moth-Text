# Moth C2.5D2R2 baseline

Apply on the existing audit branch after Luna C2.5D2R2 has been committed,
pushed, and accepted by CI.

```text
repository: adamjvr/Moth-Text
branch:     a1.1-measured-audit
HEAD:       f4941d6b7c6b2bdac5bb0b826076eaee315e75a6
```

Required committed source blob before application:

```text
Sources/MothApplication/MothApplicationShellScene.swift
15bd2a89c991ad600b17abdcf48c2d8e9b6c94d2
```

The Moth overlay contains no `Dependencies/Luna-UI/**` files. The real
submodule must be advanced explicitly to the newly committed Luna SHA before
applying the Moth patch.
